# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
  # The following environment variable is required for Google Authentication to work
  # in production on Firebase App Hosting.
  # https://firebase.google.com/docs/auth/web/google-signin#web-version-9_1
  env:
    - variable: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
      value: ${__FIREBASE_AUTH_DOMAIN__}
